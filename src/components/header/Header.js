import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <nav>
                <a href="#home">Home</a>
                <a href="#projects">Projects</a>
            </nav>
        </header>
    )
}

export default Header;
